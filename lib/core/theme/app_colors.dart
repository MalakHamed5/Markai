import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFF3F80FF);
  static const primaryLight = Color(0xFF3F80FF);
  static const primaryDark = Color(0xFF3F80FF);

  static const secondary = Color(0xff001640);
  static const secondaryLight = Color(0xff001640);
  static const secondaryDark = Color(0xff001640);

  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFFFFFFFF);
  static const textHint = Color(0xFF929BAB);

  static const darkTextPrimary = Color(0xFFEEF2FF);
  static const darkTextSecondary = Color(0xFF67687E);
  static const darkTextHint = Color(0xFF929BAB);

  static const darkBg = Color(0xFF1C1C1E);
  static const darkSurface = Color(0xFF2C2C2E);
  static const darkCard = Color(0xFF3A3A3C);
  static const darkBorder = Color(0xFF48484A);

  static const lightBg = Color(0xFFEEF2FF);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFE8EEFF);
  static const lightBorder = Color(0xFFDDE3FF);

  static const success = Color(0xFF34C759);
  static const error = Color(0xFFFF3B30);
  static const warning = Color(0xFFFF9500);
  static const online = Color(0xFF34C759);
  static const offline = Color(0xFF8E8E93);

  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
}
